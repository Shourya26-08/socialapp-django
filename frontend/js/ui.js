function initials(name){return (name||'?').slice(0,2).toUpperCase()}function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function timeAgo(d){const s=Math.max(0,(Date.now()-new Date(d))/1000);if(s<60)return `${Math.floor(s)}s`;if(s<3600)return `${Math.floor(s/60)}m`;if(s<86400)return `${Math.floor(s/3600)}h`;return `${Math.floor(s/86400)}d`}function showError(el,e){el.innerHTML=`<div class="error-banner">${escapeHtml(e.message||e)}</div>`}function showToast(msg){const x=document.createElement('div');x.className='toast';x.textContent=msg;document.body.appendChild(x);setTimeout(()=>x.remove(),2500)}function requireLoginOrRedirect(){if(!Api.isLoggedIn()){location.href='login.html';return false}return true}function renderNav(active){const n=document.getElementById('nav-slot');if(!n)return;if(Api.isLoggedIn()){const u=Api.getUsername();n.innerHTML=`<a href="index.html" class="${active==='home'?'active':''}">Feed</a><a href="profile.html?u=${encodeURIComponent(u)}">@${escapeHtml(u)}</a><button id="logout-btn" class="nav-button">Log out</button>`;document.getElementById('logout-btn').onclick=async()=>{await Api.logout().catch(()=>{});Api.clearToken();location.href='login.html'} }else n.innerHTML=`<a href="login.html">Log in</a><a href="register.html">Sign up</a>`}
